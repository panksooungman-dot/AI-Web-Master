// install.ps1이 "npm install -g" 직전에 이 파일을 실제 git 커밋 해시로
// 잠깐 덮어써 패키징한 뒤(설치 완료 후 원래 내용으로 복원), 설치된
// 패키지 안에는 그 커밋 해시가 그대로 남는다. `ai --version`이 이 값을
// 함께 표시해, "설치된 CLI가 실제로 어느 커밋에서 만들어졌는지"를
// 소스 저장소에 접근하지 않고도 즉시 확인할 수 있게 한다(단순 semver
// 버전 번호만으로는 다음 기능 추가 전까지 구분이 안 되는 문제를 보완).
// git 저장소 밖에서 소스를 직접 실행하는 경우(node bin/ai.js) 등에는
// 아래 기본값(commit: null)이 그대로 쓰인다.
export const commit = null;
