<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 260 64" width="260" height="64" role="img" aria-label="{{projectName}} logo">
  <rect width="64" height="64" rx="14" fill="{{colorPrimary}}" />
  <text x="32" y="43" font-family="system-ui, -apple-system, Segoe UI, sans-serif" font-size="30" font-weight="700" fill="#ffffff" text-anchor="middle">{{initial}}</text>
  <text x="78" y="40" font-family="system-ui, -apple-system, Segoe UI, sans-serif" font-size="26" font-weight="700" fill="{{colorForeground}}">{{projectName}}</text>
</svg>
