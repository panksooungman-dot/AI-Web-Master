// Global Vitest setup. Vitest already runs with NODE_ENV="test", so there is
// nothing to configure yet — this file is the extension point for future
// global test setup (e.g. custom matchers, MSW handlers).
export {};
